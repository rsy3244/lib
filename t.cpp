#include <bits/stdc++.h>


using namespace std;

template<class T>
class F{
	public:
	T a,b,c;
	F(): a(), b(), c(){}
	F(T a_, T b_, T c_): a(a_), b(b_), c(c_){}
};

signed main(){
	vector<int> a(2);
	cout << typeid(begin(a)).raw_name() << endl;

	return 0;
}

